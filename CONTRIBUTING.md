# Architecture v5 설계 협업 가이드

## 1. 브랜치와 PR

전체 설계 초안의 통합 브랜치는 `docs/architecture-v5-review`입니다.

1. 파트 담당자는 통합 브랜치에서 자신의 브랜치를 생성합니다.
2. 브랜치 이름은 `review/<domain>` 형식을 사용합니다.
3. PR base는 `main`이 아니라 `docs/architecture-v5-review`로 지정합니다.
4. 통합 브랜치와 `main`에 직접 push하지 않습니다.
5. 전체 Draft PR만 최종 검토 후 `main`으로 병합합니다.

예시:

```bash
git fetch origin
git switch docs/architecture-v5-review
git pull --ff-only
git switch -c review/static-context
```

## 2. 문서 소유와 중앙 통합

각 파트 담당자는 자신의 번호 문서와 Wiki 요약을 우선 검토합니다. 다음 중앙 파일은 충돌을 방지하기 위해 PM·아키텍처 담당자가 최종 통합합니다.

- `README.md`
- `docs/architecture-v5/README.md`
- `docs/architecture-v5/01-system-overview.md`
- `docs/architecture-v5/08-lightweight-data-contracts.md`
- `docs/architecture-v5/13-architecture-diagrams.md`
- `docs/architecture-v5/wiki/diagrams.md`

중앙 계약 변경이 필요하면 파트 PR에 생산자·소비자 영향과 제안 필드를 작성합니다. PM 담당자는 양쪽의 승인을 받은 후 중앙 파일을 반영합니다.

## 3. 필수 리뷰

각 PR에는 최소 다음 검토가 필요합니다.

- 담당자 self-review
- 입력을 제공하는 upstream 파트 리뷰 1명
- 결과를 소비하는 downstream 파트 리뷰 1명
- 중앙 계약·권한 경계 변경 시 PM·아키텍처 리뷰
- 보안 또는 외부 공개 경계 변경 시 Gate/동적검증 담당 리뷰

본인 혼자 작성하고 승인한 PR은 병합하지 않습니다.

## 4. 리뷰 심각도

- `[BLOCKER]`: 전체 흐름, 권한 또는 안전 경계가 성립하지 않음
- `[HIGH]`: 파트 간 계약 충돌, 잘못된 verdict/report 경로 또는 중요한 누락
- `[MEDIUM]`: 구현 전에 결정해야 하는 모호성 또는 평가 부족
- `[LOW]`: 표현, 예시, 문서 가독성
- `[QUESTION]`: 설명이나 근거 요청

병합 전 Blocker/High는 0이어야 합니다. Medium/Low는 `OPEN_QUESTIONS.md` 또는 후속 Issue에 남길 수 있습니다.

## 5. PR에서 반드시 설명할 내용

- 영향받는 정본 pipeline 단계
- 변경한 역할의 책임과 금지 권한
- 입력·출력 record와 상태 변화
- 오류·HOLD·timeout·budget 처리
- upstream/downstream 영향
- 보안·자격 증명·sandbox·공개 경계
- Wiki와 Mermaid 반영 여부
- 검증한 시나리오와 남은 질문

## 6. 전체 시나리오 검토

파트별 검토가 끝나면 아래 예시가 시스템 안에서 어떻게 처리되는지 문서만 보고 처음부터 끝까지 따라가 봅니다. 각 예시에서 입력, 담당 Agent, 판정, 다음 단계와 저장되는 결과를 확인합니다.

| 예시 상황 | 쉽게 설명하면 | 확인할 결과 |
|---|---|---|
| 1. 미리 정한 반증 조건이 확인됨 | “이 조건이 확인되면 취약점이 아니다”라고 미리 정해 두었고, 실제 코드에서 그 조건이 확인된 경우입니다. 예를 들어 사용자 입력이 위험한 함수에 도달하기 전에 항상 안전하게 차단됩니다. | 검증 Agent는 어떤 반증 조건과 코드 근거를 확인했는지 기록하고 `FALSE`로 판정합니다. 단순히 정보가 부족하거나 도구가 실패했다는 이유로 `FALSE`를 만들면 안 됩니다. |
| 2. 보류했던 가설을 다른 취약점이 풀어 줌 | 첫 번째 가설은 공격에 필요한 조건이 부족해 `HOLD`, 즉 판단 보류 상태였습니다. 이후 다른 취약점이 그 부족한 권한이나 접근 능력을 제공하는 것으로 확인된 경우입니다. | 두 결과를 바로 하나의 확정 취약점으로 합치지 않습니다. 연결 가능한 이유를 기록하고 새로운 연계 가설을 만든 뒤, 그 가설을 처음부터 다시 검증합니다. |
| 3. Docker에서 전체 공격 흐름 재현 성공 | 정적 근거만으로 끝내지 않고 격리된 Docker 환경에서 입력부터 실제 영향까지 전체 과정을 재현했습니다. `PoC`는 이 재현 절차와 관찰 결과를 정리한 자료입니다. | 재현 환경, 실행 단계, 관찰 결과와 현재 가설의 연결을 기록합니다. 검증 Agent가 이를 다른 코드 근거와 함께 종합해 `TRUE` 여부를 결정하며, 재현 성공만으로 자동 공개하지 않습니다. |
| 4. 취약점은 맞지만 공식 정책을 확인할 수 없음 | 코드상 취약점은 `TRUE`이지만, 버그바운티의 공식 범위나 허용 규칙을 가져오지 못한 경우입니다. | 정책 판단은 `UNCERTAIN`, 즉 확인 불가로 남기고 보고서 전달 권한은 `DENY`, 즉 허용하지 않음으로 둡니다. 정책을 LLM의 기억이나 추측으로 채우지 않으며 Reporter Agent를 호출하지 않습니다. |
| 5. 기술 검토 Agent가 보완을 요구함 | 최종 기술 검토 단계에서 근거 연결, 취약점 분류 또는 재현 자료가 부족해 `REVISE`, 즉 수정 후 재검토가 필요하다고 판단한 경우입니다. | 구체적인 보완 요청과 횟수를 기록하고 검증 Agent 또는 추가 탐색 Agent로 돌려보냅니다. 새 근거가 추가되기 전에는 다음 정책 검토나 보고서 작성으로 넘어가지 않습니다. |
| 6. LLM 연결 또는 로그인 실패 | LLM 서비스의 로그인 세션이 만료되었거나 API 인증에 실패해 Agent 호출 자체가 완료되지 않은 경우입니다. | 이를 “취약점이 아님”을 뜻하는 `FALSE`로 바꾸지 않습니다. `AUTH_REQUIRED` 같은 별도 오류로 저장하고, 허용된 재인증·재시도·provider 전환 정책에 따라 처리합니다. provider가 바뀌면 그 이유와 새 호출을 기록합니다. |

### 용어 빠른 설명

- `TRUE`: 현재 근거로 취약점이 성립한다고 판정한 상태
- `FALSE`: 미리 정한 반증 조건과 실제 근거로 취약점이 성립하지 않는다고 판정한 상태
- `HOLD`: 정보나 실행 환경이 부족하여 판단을 보류한 상태
- `PoC`: 취약점이 어떻게 재현되는지 보여 주는 절차와 증거
- `REVISE`: 부족한 근거를 보완한 뒤 다시 검토하라는 결과
- `UNCERTAIN + DENY`: 공식 정책을 확인하지 못해 판단을 확정하지 않고 보고서 전달도 허용하지 않는 상태

## 7. 설계와 구현의 분리

현재 설계 변경 PR에 runtime 구현을 섞지 않습니다. 설계가 승인되면 별도 구현 Issue와 PR을 만듭니다. 검증되지 않은 비용·정확도 개선이나 provider 지원 완료를 주장하지 않습니다.
